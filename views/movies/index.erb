<!DOCTYPE html>
<html lang="en">
  <head>
  <title>Movies</title>
    <!--<link rel="stylesheet" href="/home.css" />-->
  </head>


  <body>
<h1> Movies </h1>
<thead>
     <tr>
      <th>Movie</th>
      <th>Year</th>
      <th>Rating</th>
      <th>Genre</th>
      <th>Studio</th>
    </tr>
  </thead>
    <tbody>
    <% @movies.each do |movie| %>
      <div><tr>
        <td><a href="<%= "/movies/#{movie["id"]}" %>"><%= movie["title"] %></a></td>
        <td><%= movie["year"] %></td>
        <td><%= movie["rating"] %></td>
        <td><%= movie["genre"] %></td>
        <td><%= movie["studio"] %></td>
      </tr></div>
    <% end %>
    <br>
    <a href=" <%= "/movies?page=#{@next_page_num}" %> ">Next Page</a>

    <!--<a href=" <%= "/movies?page=#{@next_page_num}" %> ">Movies by Rating</a>-->
  </tbody>
  </body>
  </html>
